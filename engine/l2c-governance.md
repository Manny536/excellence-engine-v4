# L²_C governance (V4)

Love-Squared Coherence is the field gate carried from Excellence Engine V3 / KakeyaLogic.

```text
E = L² × β × C × P × h
R = d · c · e · h
```

V4 uses L²_C as:

- coherent non-sticky motion under correction;
- input to `EEV4-valid` with β_continuity and HELD.

Primary field docs remain on KakeyaLogic (`l2c_probe.py`, field notes). This file is the V4 governance binding only.
