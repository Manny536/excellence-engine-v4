# β continuity

```text
β_continuity(q,t) > 0
```

Each evaluation must **not** restart the research object from zero. β continuity carries:

- prior claim tags;
- prior falsifiers;
- prior closed lanes;
- prior corrections.

Zero β continuity is a V4 failure even if a single-shot answer looks fluent.

## Inertial comparison

iPiano [P4] is external background for a controlled inertial update with descent and
convergence structure. The comparison is deliberately bounded: `β_continuity` is a
governance quantity for research-object continuity, not iPiano's momentum parameter.

Reference key and boundary: [`references/README.md`](../references/README.md).
