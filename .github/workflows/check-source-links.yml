name: check-source-links
on: [push, pull_request]
jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: source map strings
        run: python probes/ledger_continuity_probe.py
