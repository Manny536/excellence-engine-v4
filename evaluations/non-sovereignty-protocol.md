# Non-sovereignty protocol

No evaluator may crown its own conclusion.

## Procedure

1. Require the trace to record `h_eval.bound = "< 1"` and `passed = true`.
2. Keep HELD states separate from truth statuses.
3. Treat Grok, Claude, and Solance readings as distinct evidence under II.1.
4. Require external claim-status and source registers for publication.

Fail when model agreement promotes RH or Coleman, when HELD is used as proof status,
or when an evaluator can erase counterevidence. The OPEN guard is executable as
`python probes/open_status_probe.py`.
